{% liquid
  assign product = closest.product
  assign resolved_section_name = section_name | default: 'product-list'
-%}

{% render 'product-card', product: product, section_name: resolved_section_name %}

{% schema %}
{
  "name": "t:names.product_card",
  "tag": null
}
{% endschema %}
